// server/controllers/attachmentController.js
// Placeholder for file upload handling. In production, use multer or a cloud SDK.

function uploadPlaceholder(req, res) {
  // This is just a placeholder. Implement multer or other upload middleware for real uploads.
  res.status(501).json({ error: 'not implemented' });
}

module.exports = { uploadPlaceholder };
